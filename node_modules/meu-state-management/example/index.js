var Store = require("meu-state-management");
console.log(Store);
/*
var reducer = (state: State = {name: "", age: "",  books: []}, action: Action): State => {
    if (action.type == "SET") {
        return {...state, ...action.payload};
    } else if (action.type == "DELETE") {
        return {name: "", age: "",  books: []}
    } else {
        return state;
    }
}

var store: Store = new Store({}, reducer);

var action = {
    type: "SET",
    payload: {name: "Yamada", age: 18, books: ["basic english 1", "basic english 2"]}
}

store.dispatch(action);*/ 
